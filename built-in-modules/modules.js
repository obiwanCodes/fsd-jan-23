const name = "zalmai zazai"

const supervisor = {
    name: "jaishree",
    age: 75
}

const sayHi = (name) => {
    console.log(`Hello there ${name}`)
}

module.exports = { name, supervisor, sayHi };
// export  { name, supervisor, sayHi }